# OpenAI API Configuration (Required if using OpenAI provider)
# Get your API key from: https://platform.openai.com/api-keys
# Used for: gpt-5-mini chat, GPT-4o vision, DALL-E 3 images, Whisper STT, TTS-1-HD
OPENAI_API_KEY=your_openai_api_key_here

# OpenAI Moderation Keys (UNLIMITED - optional, for high-traffic)
# Used for content moderation before ANY provider (OpenAI, Anthropic, etc.)
# Moderation is FREE but has rate limits - add more keys to scale
# System AUTOMATICALLY DETECTS all keys - just add them sequentially!
# OPENAI_MODERATION_KEY_1=your_moderation_key_1
# OPENAI_MODERATION_KEY_2=your_moderation_key_2
# OPENAI_MODERATION_KEY_3=your_moderation_key_3
# ... add more as needed (no limit!)
# Falls back to OPENAI_API_KEY if no dedicated moderation keys are set

# Anthropic API Configuration (Required if using Anthropic provider)
# Get your API key from: https://console.anthropic.com/
# Used for: Claude chat, image analysis, web search

# Option 1: Single key (simple setup)
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Option 2: Multiple primary keys (UNLIMITED - high-traffic setup)
# Use numbered keys for round-robin load distribution
# System AUTOMATICALLY DETECTS all keys - just add them sequentially!
# Add as many as you want: _1, _2, _3, ... _50, _100, etc.
# ANTHROPIC_API_KEY_1=your_primary_key_1
# ANTHROPIC_API_KEY_2=your_primary_key_2
# ANTHROPIC_API_KEY_3=your_primary_key_3
# ... add more as needed (no limit!)

# Emergency fallback keys (UNLIMITED - optional)
# Only used when ALL primary keys are rate limited
# Add as many as you want: _1, _2, _3, ... etc.
# ANTHROPIC_API_KEY_FALLBACK_1=your_fallback_key_1
# ANTHROPIC_API_KEY_FALLBACK_2=your_fallback_key_2
# ANTHROPIC_API_KEY_FALLBACK_3=your_fallback_key_3
# ... add more as needed (no limit!)

# Queue Configuration (Optional)
# Controls concurrent AI API requests to prevent rate limiting
# QUEUE_MAX_CONCURRENT=50  # Max simultaneous requests (default: 50)
# QUEUE_TIMEOUT_MS=30000   # How long to wait for a slot (default: 30s)

# Site Configuration
NEXT_PUBLIC_SITE_URL=https://jcil.ai

# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url_here
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here

# Optional: Rate Limiting (Upstash Redis)
# UPSTASH_REDIS_REST_URL=your_upstash_redis_url_here
# UPSTASH_REDIS_REST_TOKEN=your_upstash_redis_token_here

# Optional: Analytics
# NEXT_PUBLIC_ANALYTICS_ID=your_analytics_id_here

# Perplexity API Configuration (for accurate real-time search + fact-checking)
# Get your API key from: https://www.perplexity.ai/settings/api
# Used for: Real-time web search (time, weather, news, prices, fact-checking)
# Perplexity is the ONLY search provider (no Anthropic native search fallback)
#
# DUAL-POOL SYSTEM (same as Anthropic):
# Option 1: Single key (backward compatible)
PERPLEXITY_API_KEY=your_perplexity_api_key_here
#
# Option 2: Multiple keys with round-robin (recommended for high traffic)
# PERPLEXITY_API_KEY_1=your_first_key
# PERPLEXITY_API_KEY_2=your_second_key
# PERPLEXITY_API_KEY_3=your_third_key
# ... add as many as needed (no limit!)
#
# Option 3: Fallback pool (emergency reserve)
# PERPLEXITY_API_KEY_FALLBACK_1=your_fallback_key
# PERPLEXITY_API_KEY_FALLBACK_2=your_fallback_key_2
# ... add as many as needed

# Stripe Configuration
# Get your keys from: https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=your_stripe_secret_key_here
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=your_stripe_publishable_key_here
STRIPE_WEBHOOK_SECRET=your_stripe_webhook_secret_here

# Stripe Price IDs (create products in Stripe Dashboard)
# Plan pricing:
# - free: $0/mo (no Stripe price needed)
# - plus: $18/mo
# - pro: $30/mo
# - executive: $99/mo
STRIPE_PRICE_ID_PLUS=price_xxxxxxxxxxxxx
STRIPE_PRICE_ID_PRO=price_xxxxxxxxxxxxx
STRIPE_PRICE_ID_EXECUTIVE=price_xxxxxxxxxxxxx

# Stripe Coupon for Plus tier (optional - auto-applies 50% off first month)
# Create a coupon in Stripe Dashboard: Products > Coupons > +New
# Set: 50% off, Duration: Once (first month only)
# Copy the Coupon ID and paste here
STRIPE_COUPON_PLUS_FIRST_MONTH=your_coupon_id_here
